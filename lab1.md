任务1：
1. 为什么第一条指令是设置栈指针？
在进入 C 语言代码之前，必须先准备好栈。
C 语言函数调用需要依赖栈（保存返回地址、局部变量、保存寄存器等）。
如果 sp 没有正确设置，调用 C 函数会导致严重错误（比如写到未定义的内存，造成崩溃）。
所以，设置栈指针是任何 C 代码运行之前必须做的第一件事。

2. la sp, stack0 中的 stack0 在哪里定义？
我这里的栈起始标签为CPU_stack。
它在链接脚本start.c里定义，表示栈的起始位置。

3. 为什么要清零 BSS 段？

BSS 段存放的是未初始化的全局变量和静态变量。
未显式初始化的全局变量和静态变量必须默认为 0。
但是，程序加载到内存时，BSS 段的内存是 未定义值（可能是旧数据）。
因此，内核启动时必须显式把 BSS 段清零，否则 C 程序访问这些变量会出错。

4. 如何从汇编跳转到 C 函数？
RISC-V 提供 call 指令，和普通函数调用一样使用。
例如：
call start
链接器会把符号 start 解析为 C 语言函数 void start(void) 的地址。
所以，只要在汇编里声明函数符号，直接 call 即可跳转到 C 代码。

1) ENTRY(_entry) 的作用是什么？
指定可执行文件的 入口符号。
也就是“程序从哪条指令开始执行”，这里是 entry.S 里定义的 _entry 标签。

2) 为什么代码段要放在 0x80000000？
在 QEMU 的 virt 机器上，物理内存从 0x80000000 开始；用 -kernel 直接引导时，QEMU 会把 PC 跳到这里。
把 . 设为 0x80000000 让链接器按这个物理地址进行重定位，生成的内核包含正确的绝对地址/偏移，上电后即可从该地址执行，避免因地址不匹配导致的崩溃。

3) etext、edata、end 符号有什么用途？
这些是段边界的“锚点”：
etext：位于 .text 结束处（脚本里 PROVIDE(etext = .); 放在 .text 段末）。
用途：统计代码大小；设置内核文本的权限（只读/可执行）；有时用于拷贝/校验代码范围。
edata：通常放在 .data 结束处（很多系统在 .data 段末加 PROVIDE(edata = .);）。
用途：它常等于 BSS 的起始地址，因此可用 [edata, end) 作为 BSS 清零范围；也可用于计算已初始化数据的大小。


任务2：
1.启动流程
kernel.ld将entry.S中的_entry标签处设为链接成的可执行文件的入口。
_entry在分配好栈空间后调用start.c中的start函数，
start函数使用mret从M特权级变为S特权级，同时将main函数的地址设置为返回地址，从而进入main函数，完成启动。
2.内存布局
0x80000000 ────────────────────────────────
   [ .text 段 ]   内核代码（entry.S + C 函数）
──────────────────────────────────────────
   [ .rodata ]    只读数据（字符串常量）
──────────────────────────────────────────
   [ .data ]      已初始化的全局变量
──────────────────────────────────────────
   [ .bss ]       未初始化的全局变量
──────────────────────────────────────────
   end = 内核末尾
3.栈实际声明在start.c中，所以因为是全局变量，没有初始化，所以这个数组会被放在 .bss 段（未初始化全局变量区）
然后栈大小被设置为每个cpu 4kB

4.需要清零.bss段。不然BSS 段的内存是 未定义值，可能导致错误。必须手动置零。

5. 最简单串口输出需要配置哪些寄存器？

对于 QEMU virt 平台（UART 16550a，基地址 0x10000000）：
LSR(Line Status Register)
THR (Transmit Holding Register)：往这里写一个字节，就能输出字符。

