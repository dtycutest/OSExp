# lab2 页表与内存管理
## Sv39页表机制
#### 39位虚拟地址的 分解
VPN[2]  |  VPN[1]  |  VPN[0]  |   offset 
9 | 9 | 9 |12   (位数)
每一个VPN段9位，代表一级页表索引。9位是因为一个页（4kB）正好可以存放512个页表项PTE(8B)。
#### 页表项的格式
物理页号 | (10位)标志位
#### 思考
每一个页表都是内核分配的物理页。每一次在映射虚拟地址和物理地址过程中都要分配还没有产生的页表，以保证多级页表的结构。三级页表符合当前系统可用的内存大小。四级页表会导致过多不必要的页表开销，二级页表则会导致可索引的虚拟地址大小太小。

## 物理内存分配器
#### xv6的kalloc.c
采用链表的数据结构来维护可使用（未分配）的页面。初始化时将所有可分配的内存空间按页划分，一个一个连成链表结构。分配时取链表头的页分配，更新链表头；释放时，将释放的页重新挂在链表头，更新链表头。
链表的结构，将链表的next指针直接作为页的起始地址的指针。相当于在每一个页的开头存放了下一个可分配页的起始地址。这样最省空间。

#### 我实现的物理内存分配器
我目前采用了和xv6同样的链表结构，以及尽可能相似的命名风格。然后根据老师的要求将可分配的内存空间分为内核去和用户区2个部分，并且为每一个区维护了更多的信息：起始地址，结束地址，可分配页数量。
具体实现上，就是将原来的一个链表分为两个负责不同内存区域的链表。其中起始和结束地址在初始化的时候给定。给分配页和释放页的函数增加一个参数in_kernel代表操作的是否是内核区的页，然后在函数内根据需要维护不同的链表，同时维护可分配页的数量。

## 页表管理
#### xv6的页表管理
walk函数，即根据给出的顶级页表和虚拟地址寻找对应的页表项。如果alloc参数为1，则在寻找过程中分配新的页表（如果有为索引的页表项）。具体来说，首先根据虚拟地址中第2级索引取出的9位信息，在当前页表中找到页表项，判断页表项是否有效（无效则根据情况分配），根据页表项找到对应的物理页，作为新的页表，重复这个过程。最终找到一个页表项，对应的就是最终的物理页。
mappages，将一定长度的虚拟地址和物理地址映射起来，具体来说就是将每一个虚拟页通过walk找到对应的页表项，并将对应物理页的页号和设置的权限位填入。
unamp，释放某虚拟页，就是将该页对应的物理页释放，并将对应的页表项清空。

#### 我的页表管理实现
大体也与xv6的页表实现类似。
主要的不同在于：由于我们在物理内存分配中划分了内核区和用户区，我们必须修改一部分实现。我们将所有页表的分配实现为内核区页面的分配。而将所有虚拟页的unmap操作，认为是释放用户去的物理页。所有的页表都在内核区中，而多级页表结构中的需要释放的叶子页面都是是用户区的页面，我认为这样预设是合理的。因为内核部分映射的物理页大多都是不会进行释放的。
从流程上讲，我们首先要进行内核顶级页表的初始化kvminit。我们需要分配一个顶级页表，然后清零。清零这点很重要因为xv6原本的实现中分配物理页会给每个字节填入不为0的垃圾信息，会导致页表中页表项的PTE_V为1，从而引发错误。然后我们需要将原本内核使用的一些内容的物理地址与虚拟地址进行映射（这里物理地址和虚拟地址传入相同的值，就可以在不影响任何原先使用的情况下完成映射）。然后我们进行kvminithart，将顶级页表的地址传入对应的satp寄存器（在这前后清空快表），开启分页，此后系统所有的地址访问都会先理解成虚拟地址然后通过页表找到物理地址再访问。


## 测试
测试代码都是发在群里的代码
#### 内存第一阶段测试
测试结果：
```
mem = 0x0000000080c15000, data = 16843009
mem = 0x0000000080c14000, data = 16843009
mem = 0x0000000080c13000, data = 16843009
mem = 0x0000000080c12000, data = 16843009
mem = 0x0000000080c11000, data = 16843009
mem = 0x0000000080c10000, data = 16843009
mem = 0x0000000080c0f000, data = 16843009
mem = 0x0000000080c0e000, data = 16843009
mem = 0x0000000080c0d000, data = 16843009
cpu 1 alloc over
mem = 0x0000000080c0c000, data = 16843009
cpu 0 alloc over
cpu 1 free over
cpu 0 free over
```
和word文档中的预期结果相近。分配物理页面和释放都没有出现问题。
#### 内存第二阶段测试
测试结果(关键部分)：
```
test-1
pte 0: 0x00000000203f0801  0x80fc2 0x1  //页表项   物理页号  FLAGS
pte 1: 0x00000000203efc01  0x80fbf 0x1
//···
pte 255: 0x00000000203ef401  0x80fbd 0x1
//···
```
测试结果只输出了测试页表的顶级页表的页表项内容，pte0代表虚拟地址0,PGSIZE* 10,PGSIZE* 512映射成功。pte1代表虚拟地址PGSIZE* 512*512映射成功。pte255代表虚拟地址MAXVA - PGSIZE映射成功（MAXVA为(1L << (9 + 9 + 9 + 12 - 1))）。以上虚拟地址对应顶级页表中的页表项位置可以根据其第二级索引的9位得出。
测试结果的第二部分释放虚拟页并不会影响顶级页表。第二部分第一个mappages操作会导致panic:remap，注释之后再运行系统不再会报错。